name: Spellcheck Action
on: push

jobs:
  build:
    name: Spellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: apt install aspell
    - name: run spellcheck
      run: sh spellcheck.sh
